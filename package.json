{
  "name": "blackhole",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ justliquidity/blackhole",
    "create-local": "graph create --node http://localhost:8020/ justliquidity/blackhole",
    "remove-local": "graph remove --node http://localhost:8020/ justliquidity/blackhole",
    "deploy-local": "graph deploy --node http://localhost:8020/ --debug --ipfs http://localhost:5001 justliquidity/blackhole"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
